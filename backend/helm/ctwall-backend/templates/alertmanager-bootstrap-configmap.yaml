apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ctwall-backend.fullname" . }}-alertmanager-bootstrap
  labels:
    {{- include "ctwall-backend.labels" . | nindent 4 }}
data:
  alertmanager.yml: |
{{ .Values.alertmanager.bootstrapConfig | indent 4 }}
